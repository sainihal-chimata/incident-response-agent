[project]
name = "incident-response-agent"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "pydantic",
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "app:main"
